---

---

<section class="relative py-16 border-y border-dark-border/50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <p class="text-center text-sm font-medium text-light-text/30 uppercase tracking-widest mb-10">
      Trusted by teams at
    </p>
    <div class="flex flex-wrap items-center justify-center gap-x-12 gap-y-8 opacity-40">
      <!-- Company logos represented as text (replace with real logos) -->
      {['TechCrunch', 'Product Hunt', 'YCombinator', 'Forbes', 'Wired', 'The Verge'].map((name) => (
        <span class="text-xl font-bold text-light-text/60 hover:text-light-text/80 transition-colors cursor-default">
          {name}
        </span>
      ))}
    </div>
  </div>
</section>
